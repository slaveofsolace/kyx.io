# Inkfall Foundry current authoritative traversal matrix

Status: **BOUNDED DETERMINISTIC PASS; G5 REMAINS OPEN**

This slice extends the exact west-Archive rounding regression from commit
`8dc762d` into a current, reproducible map traversal matrix while preserving
the accepted Phase 3 movement profile and fail-closed collision semantics.

## Coverage

The current native revision-2 automated authority suite was rerun twice:

- 2/4/8 synthetic-player scenarios: `PASS / PASS / PASS`;
- 14/14 agents complete;
- 70/70 route tapes complete;
- 20/20 governing route metrics inside their bands;
- zero embeds, snags, retained-player overlaps, recovery-volume entries, and
  kill-volume entries;
- recovery, kill-priority, and post-recovery escape probes pass for every
  arrangement;
- suite hash `143d09532b9c2b24`, with byte-identical repeated results.

The explicit non-default revision-3 candidate now has two additional bounded
surfaces:

1. The canonical Press-junction repair retains its exact revision-2
   fail-closed reproduction, exact revision-3 pass, and seven adjacent lanes
   with zero recovery and overlap.
2. The west-Archive face now has nine approach probes, including the exact
   runtime-v45 state and two explicitly seeded-overlap recovery diagnostics,
   followed by four clear escape vectors. All 13 moves pass, every final
   capsule is clear, both seeded overlaps recover, and no recovery fails.

The exact runtime-v45 move remains `(145, 0, -39)` mm from feet position
`(-24024, 37, 424)` mm. It applies `(66, 0, -22)` mm after two reported
contacts and finishes clear at `(-23958, 37, 402)` mm.

## Current regression truth

The focused/adjacent matrix passes 70/70 tests across seven files. Application,
simulation-source, and simulation TypeScript pass; focused lint passes; the
asset validator reports 0 errors and the same 33 legacy warnings; the
production build passes with the existing large-chunk advisory.

The post-`8dc762d` adapter no longer throws the historical revision-1
eight-player depenetration exception at tick 91. The same frozen route now
continues until a deterministic snag at tick 137. The revision-1 suite and
scenario hashes were refreshed to current truth, while its 8-player result
remains `FAIL` with 16 issues and no topology mutation authorization.

## Evidence

Evidence root:
`evidence/2026-07-25/phase-6-g5-authoritative-traversal-matrix-v1`

The initial consistency verifier passed 14/14 checks. Its proof SHA-256 was
`45b140c5f4eef585857b3f732986283bf5f615bd7003906a4eeef568bb7ace43`.
That first capture remains historical evidence rather than an immutable source
closure because its new capture/test files were untracked and unhashed.

## Provenance-v2 closure contract

The hardened recapture closes the exact capture runner/module, verifier,
focused regression, governing collision conversion and movement sources,
revision-2/revision-3 collision and fixture inputs, map manifests, route/spawn
fixtures, package manifest, dependency lock, TypeScript configs, and lint
config by byte hash.

It also records:

- exact `HEAD` and branch;
- the complete non-evidence dirty index/worktree as status, binary-diff hashes,
  and per-file byte hashes;
- an explicit exclusion of this generated G5 evidence directory to avoid a
  circular self-manifest;
- raw stdout/stderr, exit code, duration, and output hashes for the focused
  capture, verifier, 70-test matrix, typechecks, lint, and asset validation;
- a separate evidence baseline that closes every substantive generated
  provenance-v2 artifact without attempting to hash itself.

The west-Archive `successfulRecoveryCount` is a bounded inferred observation:
the deterministic probe begins overlapping, `moveCapsule` returns, and the
post-move capsule is clear. It is not an authority telemetry counter and no
production recovery telemetry is claimed.

## Nonclaims and remaining blockers

This is automated deterministic authority evidence only. The 2/4/8 suite is
revision 2; the revision-3 evidence is limited to the canonical junction and
west-Archive probes. Revision 3 is not the product default or promoted.

G5 still requires an exact product-browser revision-3 route replay, human
2/4/8-player sessions, spawn/route/counterplay/fun/readability review, and the
remaining product-map, final-art, and performance acceptance. No commit, push,
deployment, promotion, or gate acceptance occurred in this slice.

*Currently being worked on.*
