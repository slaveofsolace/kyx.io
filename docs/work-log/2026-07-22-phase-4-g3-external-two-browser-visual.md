# Phase 4 G3 external two-browser visual record

Status: `EXTERNAL_TWO_BROWSER_VISIBLE_AUTHORITY_MANUAL_PASS`; G3 remains `G3_NOT_ACCEPTED`.

Two isolated Chrome contexts loaded the local visible authority observer against the ephemeral external HTTPS/WSS Worker build `g3tmp-93ae5c5c1604`. Both displayed room `KYX-E9MTA8` and the same match identity under the `matrix-rtt-50` impairment profile.

Client A produced 7,900 mm of authoritative movement. Client B's remote-sample count advanced from 1 to 14. The explicit disconnect/resume action preserved the server-owned player and match identities, rotated the credential generation to 1, and both clients remained joined with no client, console, or page error.

The readable viewport captures are `external-a-resumed-viewport.png` and `external-b-peer-viewport.png`. Full-page records are retained alongside them, and every image byte count and SHA-256 is pinned in `manual-visual-runtime.json`.

This is a manual visual capture rather than a source-frozen independently verified harness. It uses the development evidence route, not a product online flow or production-origin authentication. It therefore supplements the independently verified direct-WSS record but does not accept G3.

An offline integrity verifier independently passed 14/14 checks covering the exact assertion contract, pinned target/build, room/match shapes, movement/resume measurements, explicit nonclaims, PNG structure/dimensions, every byte count/hash, and absence of credential fields or opaque 43-character credentials.

- Runtime SHA-256: `89f2da3216716229bac08029188b326f302cdc21dbeb0121b5685450d0aed74c`.
- Verification SHA-256: `19241a41f90f31aa59904b40ccbab62bd9db37bd93b918a519d55dd99f213fcc`.
- Verifier source SHA-256: `dafacffad253dc6790b3c7c262d0f464854d7aa08c89ad7bf678d38b25e79bff`.
