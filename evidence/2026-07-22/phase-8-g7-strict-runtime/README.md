# G7 strict rendered-runtime slice

This bounded slice turns on the repository's strict Phase 8 accessibility path and verifies the real Settings interaction sequence, keyboard focus indicator, panel focus containment, reduced-motion animation inventory, forced colors, and the named accessibility controls.

Run the evidence verifier from the repository root:

```powershell
node evidence/2026-07-22/phase-8-g7-strict-runtime/verify.mjs
```

The combined strict browser command completed with four passes, including the pointer-lock/Escape capability probe. Focused ESLint also passed.

This evidence does not close G7. Literal browser zoom was attempted through the available in-app browser, but the shortcuts did not produce a measurable browser-level zoom change. Human screen-reader acceptance also remains open.
