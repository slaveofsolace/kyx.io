# Non-secret local staging template. Copy to .env.staging and replace the
# placeholder with the isolated staging Worker origin. .env.staging is ignored.
VITE_KYX_AUTHORITY_ORIGIN=https://your-staging-worker.example.workers.dev
